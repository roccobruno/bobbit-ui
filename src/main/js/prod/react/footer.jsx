/** @jsx React.DOM */
var React = require("react");
var bootstrap = require("react-bootstrap");


module.exports = React.createClass({
    propTypes: {},
    render: function () {
        return (
            <div></div>
            );
    }
});